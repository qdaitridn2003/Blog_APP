import {
    LOCAL_HOST,
    END_POINT,
    PORT,
    ACCESS_TOKEN_KEY,
    IS_INTRO_KEY,
    IS_LOGIN_KEY,
    REFRESH_TOKEN_KEY,
} from '@env';

export const AppConfig = {
    localHost: LOCAL_HOST,
    port: PORT,
    endPoint: END_POINT,
    accessTokenKey: ACCESS_TOKEN_KEY,
    refreshTokenKey: REFRESH_TOKEN_KEY,
    introKey: IS_INTRO_KEY,
    loginKey: IS_LOGIN_KEY,
};
